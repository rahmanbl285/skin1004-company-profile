/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
        "skin1004.com",
        "images.unsplash.com",
        "firebasestorage.googleapis.com",
        "ucarecdn.com",
        "cdn.shopify.com",
        "randomuser.me"
        ]
    }
};

export default nextConfig;
